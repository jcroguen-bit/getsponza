# GetSponza — Codebase Documentation

## Overview
GetSponza is a creator sponsorship tool. Users paste their channel URL, get a free Sponsorship Readiness Score, and can unlock a paid sponsorship kit or a lower-cost refresh via NanoCorp's hosted payment link plus a manual email-based unlock flow on the results page.

## Tech Stack
- Next.js 16 (App Router) + TypeScript + Tailwind CSS
- PostgreSQL (Neon DB) via `pg` package
- NanoCorp hosted Stripe payment link
- Resend for transactional email
- Deployed on Vercel at https://sponza.nanocorp.app

## File Structure
```
app/
  layout.tsx              — Root layout, Google Fonts (Playfair Display + DM Sans), analytics beacon
  globals.css             — CSS variables (navy/gold/white), custom animations, font imports
  page.tsx                — Landing page hero form now calls `/api/generate-kit` directly and routes to `/results/[kit_id]`
  results/page.tsx        — Compatibility wrapper for legacy query-string result links
  results/results-client.tsx — Legacy bootstrap client that upgrades `/results?url=...` into a persistent `/results/[kit_id]` route
  results/[kitId]/page.tsx — Dynamic result route keyed by the persisted kit ID, with optional `?unlock=1` state for refresh handoff
  results/[kitId]/results-detail-client.tsx — DB-backed results UI with direct payment-link CTAs, manual unlock, download, and refresh states
  api/generate-kit/route.ts — POST endpoint — scrape, cache, gate, and return sponsorship kit output
  api/generate-pdf/route.ts — POST endpoint — returns the designed paid media kit PDF for a `kit_id`
  api/kits/[kitId]/route.ts — GET endpoint that returns the current gated paid/free view for one kit record
  api/refresh-kit/route.ts — POST endpoint that creates a new refresh kit from an eligible paid kit
  api/unlock-kit/route.ts — POST endpoint that validates `{ kit_id, email }`, marks the exact kit paid, primes the pack, and sends the confirmation email
  api/download-pack/route.ts — POST endpoint — returns the paid sponsorship-pack ZIP (`media_kit.pdf`, `pitch_emails.txt`, `brand_list.csv`)
  api/submit/route.ts     — POST endpoint — stores URL submission in Neon DB
  api/webhooks/nanocorp/route.ts — Acknowledges NanoCorp webhook delivery without auto-unlocking kits in the MVP manual flow
  checkout/success/page.tsx — Post-payment thank-you page that sends users back to the results-page unlock form
lib/
  db.ts                   — Shared Neon `pg` pool + table bootstrap for submissions, kits, unlocks, failures
  sponza/commerce.ts      — Price constants, the shared NanoCorp payment link, and refresh timing helpers
  sponza/email.ts         — Resend helper for “kit ready” confirmation emails
  sponza/utils.ts         — URL normalization, platform detection, hashing, parsing helpers
  sponza/scrape.ts        — YouTube API scraper + Instagram/TikTok public metadata fallbacks
  sponza/llm.ts           — Single GPT-4o JSON-schema prompt for full sponsorship kit generation
  sponza/service.ts       — Cache lookup, kit creation, DB-backed gating by `kit_id`, paid-state persistence, manual unlock helpers, and refresh creation
  sponza/deliverables.tsx — Server-side PDF rendering and sponsorship-pack ZIP assembly
```

## Design System
- **Colors**: `--navy: #0F1B2D`, `--warm-white: #FAFAF8`, `--gold: #F5A623`
- **Typography**: Playfair Display (display/headings) + DM Sans (body)
- **Font class**: `.font-display` → `font-family: 'Playfair Display', serif`

## Database
- Table: `submissions (id, url, email, platform, created_at)`
- Table: `sponsorship_kits (url_hash, niche_hash, cache_key, platform, full_kit_json, scrape_json, email, paid, paid_at, stripe_payment_id, stripe_checkout_session_id, refresh_source_kit_id, pack_ready_at, pack_last_error, created_at, updated_at)`
- Table: `payment_unlocks (kit_id, email, amount_cents, currency, stripe_session_id, stripe_payment_id, created_at, updated_at)`
- Table: `kit_failures (url, email, platform, cache_key, step, error_message, details, created_at)`
- Auto-created on first API hit via shared `ensureDatabase()` in [`lib/db.ts`](/home/worker/repo/lib/db.ts)
- `DATABASE_URL` is set as a protected env var on Vercel
- Cache key is `sha256(normalized_url + "::" + normalized_niche_notes)` and reuses kits for 90 days
- The cache-key index is now non-unique so a 90-day refresh can create a brand-new kit row while still reusing recent kits
- `ensureDatabase()` now backfills `paid = true` for any existing row that already had `paid_at IS NOT NULL`

## Payment
- Both the full-kit CTA and refresh purchase now use the shared NanoCorp payment link `https://buy.stripe.com/7sYfZielSbL9eP878DeP33I`
- `POST /api/unlock-kit` is the MVP unlock mechanism: it validates `{ kit_id, email }`, confirms the email matches the stored kit record, sets `paid = true`, keeps `paid_at`, primes the PDF/ZIP artifacts, and sends the existing Resend confirmation email
- The payment link opens in a new tab; unlock happens from the results page rather than from a payment webhook
- `POST /api/webhooks/nanocorp` now acknowledges deliveries but intentionally does not auto-unlock kits
- Paid gating is verified from the `sponsorship_kits` row itself, keyed by `kit_id`

## Recent Changes
- 2026-04-24: For the QA blocker task covering `/privacy`, `/terms`, and the results-page CTA label, ran `npm install` because `node_modules` was absent in this workspace, then read the bundled Next 16 docs at [`node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md), [`node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md), [`node_modules/next/dist/docs/01-app/01-getting-started/14-metadata-and-og-images.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/14-metadata-and-og-images.md), [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md), and [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md) before adding routes and metadata.
- 2026-04-24: Audited [`app/results/[kitId]/page.tsx`](/home/worker/repo/app/results/[kitId]/page.tsx), [`app/results/[kitId]/results-detail-client.tsx`](/home/worker/repo/app/results/[kitId]/results-detail-client.tsx), [`app/page.tsx`](/home/worker/repo/app/page.tsx), [`app/layout.tsx`](/home/worker/repo/app/layout.tsx), and [`app/globals.css`](/home/worker/repo/app/globals.css). Findings: the repo had no `app/privacy/page.tsx` or `app/terms/page.tsx`, and the upgrade CTA in the client-rendered results page defaulted to `TBD` during SSR because it rendered before kit data loaded.
- 2026-04-24: Added the shared legal shell [`app/legal-page-shell.tsx`](/home/worker/repo/app/legal-page-shell.tsx) plus the new routes [`app/privacy/page.tsx`](/home/worker/repo/app/privacy/page.tsx) and [`app/terms/page.tsx`](/home/worker/repo/app/terms/page.tsx). Both pages now ship creator-friendly metadata, a top-level `← Back to GetSponza` link, Inter typography via `next/font/google`, and navy/gold styling aligned to the rest of the product.
- 2026-04-24: Updated [`app/results/[kitId]/results-detail-client.tsx`](/home/worker/repo/app/results/[kitId]/results-detail-client.tsx) so the upgrade CTA now server-renders the exact MVP copy `Get Your Full Kit — $29` instead of a `TBD` placeholder before client data arrives.
- 2026-04-24: Verified `npm run lint` and `npm run build` pass after the legal-page and CTA fixes. Started the production app locally with `PORT=3100 npm run start -- --hostname 127.0.0.1`, then confirmed `curl` returns `200` for both `http://127.0.0.1:3100/privacy` and `http://127.0.0.1:3100/terms`. Attempted a local `agent-browser` UI check as well, but this environment still does not have a Chrome binary installed.
- 2026-04-23: `node_modules` was absent in this workspace at the start of the task, so `npm install` was run before making changes. After install, read the bundled Next 16 docs at [`node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md), [`node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md), [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md), and [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md) before editing the landing page and route handlers.
- 2026-04-23: Audited [`app/page.tsx`](/home/worker/repo/app/page.tsx), [`app/api/generate-kit/route.ts`](/home/worker/repo/app/api/generate-kit/route.ts), [`app/api/unlock-kit/route.ts`](/home/worker/repo/app/api/unlock-kit/route.ts), [`app/results/results-client.tsx`](/home/worker/repo/app/results/results-client.tsx), [`lib/sponza/service.ts`](/home/worker/repo/lib/sponza/service.ts), [`lib/db.ts`](/home/worker/repo/lib/db.ts), [`lib/sponza/utils.ts`](/home/worker/repo/lib/sponza/utils.ts), [`lib/sponza/deliverables.tsx`](/home/worker/repo/lib/sponza/deliverables.tsx), and [`lib/sponza/email.ts`](/home/worker/repo/lib/sponza/email.ts) for the unlock blocker. Findings: the homepage email field was optional and sent `undefined`; `/api/generate-kit` accepted missing emails; and cached kits were returned without backfilling `email` when the stored row was legacy `NULL`.
- 2026-04-23: Updated [`app/page.tsx`](/home/worker/repo/app/page.tsx) so the landing-page free-score form now requires an email address, renders the email input directly below the URL input with the placeholder `your@email.com — we'll send your kit here if you upgrade`, and always posts `{ url, email, niche_notes? }` to [`/api/generate-kit`](/home/worker/repo/app/api/generate-kit/route.ts).
- 2026-04-23: Hardened [`app/api/generate-kit/route.ts`](/home/worker/repo/app/api/generate-kit/route.ts) so free-kit generation rejects missing/invalid email values server-side, preventing future `sponsorship_kits.email = NULL` rows from direct API callers.
- 2026-04-23: Added cached-kit email backfill in [`lib/sponza/service.ts`](/home/worker/repo/lib/sponza/service.ts). When a cached kit exists for the same URL/niche and its stored `email` is `NULL`, the service now updates that row with the new submission email before generating the response, which also allows paid cached kits to resolve to the paid tier again when the correct email is supplied.
- 2026-04-23: Updated [`lib/sponza/service.ts`](/home/worker/repo/lib/sponza/service.ts) and [`app/api/unlock-kit/route.ts`](/home/worker/repo/app/api/unlock-kit/route.ts) so legacy kits with `email = NULL` now return `409` with the exact manual-support message `No email on file for this kit. Please contact sponza@nanocorp.app for manual unlock.` instead of falling through to the generic email-mismatch path.
- 2026-04-23: Added the DB-backed regression script [`scripts/test-unlock-flow.mjs`](/home/worker/repo/scripts/test-unlock-flow.mjs) and the npm script `npm run test:unlock-flow`. It seeds a cached kit with `email = NULL`, posts to the real `/api/generate-kit` route with a test email, verifies the DB row is backfilled, then posts to `/api/unlock-kit` and verifies the kit becomes paid.
- 2026-04-23: Verified `npm run lint` and `npm run build` pass after the unlock-flow changes.
- 2026-04-23: Ran `TEST_BASE_URL=http://127.0.0.1:3100 npm run test:unlock-flow` against a local `next start` server. The test inserted a cached seed kit, got a `200` free response from `/api/generate-kit`, confirmed the DB row changed from `email = NULL` to `unlock-flow-1776942292391@example.com`, then got a `200` paid response from `/api/unlock-kit` for the same `kit_id`, with `paid = true` and `paid_at IS NOT NULL` in Postgres.
- 2026-04-23: Ran one additional legacy-edge check against `/api/unlock-kit` with a seeded `NULL`-email kit and confirmed the response is `409` with `{"error":"No email on file for this kit. Please contact sponza@nanocorp.app for manual unlock."}`.
- 2026-04-21: Read the local Next 16 Route Handler and dynamic-route docs at [`node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md), [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md), and [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md) before adding the analytics route.
- 2026-04-21: Added DB-backed funnel analytics with a new `analytics_events` table bootstrapped from [`lib/db.ts`](/home/worker/repo/lib/db.ts) and a shared insert helper in [`lib/analytics.ts`](/home/worker/repo/lib/analytics.ts).
- 2026-04-21: Wired `free_score_requested` logging into [`lib/sponza/service.ts`](/home/worker/repo/lib/sponza/service.ts) so every successful free-score request records against the resolved `kit_id` plus `url_hash`.
- 2026-04-21: Added [`app/api/analytics/route.ts`](/home/worker/repo/app/api/analytics/route.ts) and updated [`app/results/[kitId]/results-detail-client.tsx`](/home/worker/repo/app/results/[kitId]/results-detail-client.tsx) so upgrade/payment CTAs record `upgrade_cta_clicked` server-side without changing the existing UI flow.
- 2026-04-21: Verified `npm run lint` and `npm run build` pass after the analytics changes.
- 2026-04-21: Ran a local smoke test against the built app by posting `upgrade_cta_clicked` to `/api/analytics`, then confirmed the row landed in Postgres via `SELECT ... FROM analytics_events`.
- 2026-04-21: Verified `npm run build` passes after adding the Vercel custom-domain alias config.
- 2026-04-21: Pushed commit `05cbd80` (`Add Vercel custom domain alias`) to `main`.
- 2026-04-21: Waited the required 90 seconds after pushing, then made the single required deployment verification attempt with `agent-browser open https://sponza.nanocorp.app`; the check could not run because this environment still does not have a Chrome binary installed.
- 2026-04-21: Confirmed this workspace remote is `nanocorp-hq/sponza`, while the task brief references `jcroguen-bit/getsponza`; if Vercel is still wired to the GitHub repo from the brief, this change must also be mirrored there or the Vercel project connection must be updated.
- 2026-04-21: Read the bundled Next 16 metadata/config docs at [`node_modules/next/dist/docs/01-app/01-getting-started/14-metadata-and-og-images.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/14-metadata-and-og-images.md) and [`node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/index.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/index.md) before the domain-config work.
- 2026-04-21: Added [`vercel.json`](/home/worker/repo/vercel.json) with `alias: ["sponza.nanocorp.app"]` so production deployments carry the requested custom-domain alias in repo config.
- 2026-04-21: Audited the repo for hardcoded deployment hosts and found no `https://getsponza.vercel.app` references in application code; no `metadataBase`, canonical URL, CSP, CORS, or `next.config.ts` host allowlist changes were required for this task.
- 2026-04-21: Verified the Vercel config schema still includes the `alias` property, but custom-domain activation still requires the domain to be added in the Vercel project settings and pointed at Vercel DNS by the domain owner.
- 2026-04-21: Installed project dependencies so the bundled Next 16 docs were available locally, then read [`node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md), [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md), and [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md) before changing the results route params and adding the new unlock route.
- 2026-04-21: Replaced the custom Stripe checkout flow with NanoCorp's shared payment link across [`app/results/[kitId]/results-detail-client.tsx`](/home/worker/repo/app/results/[kitId]/results-detail-client.tsx), removed [`app/api/create-checkout/route.ts`](/home/worker/repo/app/api/create-checkout/route.ts), removed [`app/api/webhooks/stripe/route.ts`](/home/worker/repo/app/api/webhooks/stripe/route.ts), removed [`lib/stripe.ts`](/home/worker/repo/lib/stripe.ts), and removed the `stripe` package from [`package.json`](/home/worker/repo/package.json).
- 2026-04-21: Added manual unlock handling in [`app/api/unlock-kit/route.ts`](/home/worker/repo/app/api/unlock-kit/route.ts) and [`lib/sponza/service.ts`](/home/worker/repo/lib/sponza/service.ts), including exact-kit email matching, `paid` boolean persistence/backfill, on-demand pack priming, and moving the Resend confirmation email trigger from the old Stripe webhook to the unlock route.
- 2026-04-21: Updated [`app/api/webhooks/nanocorp/route.ts`](/home/worker/repo/app/api/webhooks/nanocorp/route.ts) to acknowledge webhook delivery without auto-unlocking kits, which keeps the new MVP flow manual even if NanoCorp forwards payment events.
- 2026-04-21: Updated the results-page UX and post-payment copy across [`app/results/[kitId]/page.tsx`](/home/worker/repo/app/results/[kitId]/page.tsx), [`app/results/[kitId]/results-detail-client.tsx`](/home/worker/repo/app/results/[kitId]/results-detail-client.tsx), [`app/page.tsx`](/home/worker/repo/app/page.tsx), and [`app/checkout/success/page.tsx`](/home/worker/repo/app/checkout/success/page.tsx) so users pay in a new tab, then return to unlock via the “Already paid?” email form.
- 2026-04-21: Verified `npm run lint` and `npm run build` pass after the NanoCorp payment-link/manual-unlock refactor.
- 2026-04-21: Pushed commit `eb22232` (`Refactor payments to NanoCorp link unlock flow`) to `main`, waited the required 90 seconds, and attempted the one-pass deployment verification with `agent-browser`, but the environment still lacks a Chrome binary, so browser verification of `https://sponza.nanocorp.app` remains pending.
- 2026-04-21: Audited the full repo for branded copy before a rename sweep, read the bundled Next 16 metadata/layout docs at [`node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md), [`node_modules/next/dist/docs/01-app/01-getting-started/14-metadata-and-og-images.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/14-metadata-and-og-images.md), and [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md), and confirmed the rename should touch user-facing copy, metadata, email sender/copy, Stripe product labels, branded download filenames, and LLM prompt text while leaving internal module paths like `lib/sponza/*` and the live `sponza.nanocorp.app` deployment URL unchanged.
- 2026-04-21: Standardized visible brand references on `GetSponza` across [`app/layout.tsx`](/home/worker/repo/app/layout.tsx), [`app/page.tsx`](/home/worker/repo/app/page.tsx), [`app/results/results-client.tsx`](/home/worker/repo/app/results/results-client.tsx), [`app/results/[kitId]/results-detail-client.tsx`](/home/worker/repo/app/results/[kitId]/results-detail-client.tsx), [`app/checkout/success/page.tsx`](/home/worker/repo/app/checkout/success/page.tsx), [`app/api/create-checkout/route.ts`](/home/worker/repo/app/api/create-checkout/route.ts), and [`lib/sponza/llm.ts`](/home/worker/repo/lib/sponza/llm.ts); updated the footer/contact email to `sponza@nanocorp.app`; and changed the branded ZIP download name to `getsponza-sponsorship-pack.zip`.
- 2026-04-21: Recreated the active NanoCorp Stripe catalog under the new brand because the CLI does not expose a product-update command: created `GetSponza Sponsorship Kit` (`3a093915-aedc-42dd-9c9d-60bb8b1b2ff6`) and `GetSponza Kit Refresh` (`9b2c68e5-c664-4f8b-856e-4e0e11e8524a`) with updated descriptions, deactivated the old branded products, and confirmed the shared active payment link is now `https://buy.stripe.com/7sYfZielSbL9eP878DeP33I`.
- 2026-04-21: Verified `npm run lint` and `npm run build` pass after the GetSponza rename sweep and Stripe catalog recreation.
- 2026-04-21: Pushed commit `af36477` (`Rename brand to GetSponza`) to `main`, then attempted the required one-pass deployment check with `agent-browser` after a 90-second wait, but the environment does not have a Chrome binary installed, so browser verification of `https://sponza.nanocorp.app` is still pending.
- 2026-04-21: Installed `stripe` and `resend`, read the local Next 16 docs at [`node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md), [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md), and [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md) before adding dynamic result routes and new route handlers.
- 2026-04-21: Reworked [`lib/db.ts`](/home/worker/repo/lib/db.ts) and [`lib/sponza/service.ts`](/home/worker/repo/lib/sponza/service.ts) so kits are paid and refreshed by `kit_id`, Stripe IDs are stored directly on `sponsorship_kits`, `payment_unlocks` can record multiple payments per email, and cache-key reuse no longer blocks new refresh rows.
- 2026-04-21: Added direct Stripe endpoints [`app/api/create-checkout/route.ts`](/home/worker/repo/app/api/create-checkout/route.ts), [`app/api/kits/[kitId]/route.ts`](/home/worker/repo/app/api/kits/[kitId]/route.ts), [`app/api/refresh-kit/route.ts`](/home/worker/repo/app/api/refresh-kit/route.ts), and [`app/api/webhooks/stripe/route.ts`](/home/worker/repo/app/api/webhooks/stripe/route.ts), plus shared helpers in [`lib/stripe.ts`](/home/worker/repo/lib/stripe.ts), [`lib/sponza/commerce.ts`](/home/worker/repo/lib/sponza/commerce.ts), and [`lib/sponza/email.ts`](/home/worker/repo/lib/sponza/email.ts).
- 2026-04-21: Replaced the old query-param-only results flow with a persistent `/results/[kit_id]` experience in [`app/results/[kitId]/page.tsx`](/home/worker/repo/app/results/[kitId]/page.tsx) and [`app/results/[kitId]/results-detail-client.tsx`](/home/worker/repo/app/results/[kitId]/results-detail-client.tsx), while keeping [`app/results/results-client.tsx`](/home/worker/repo/app/results/results-client.tsx) as a compatibility bootstrap for old links.
- 2026-04-21: Updated [`app/page.tsx`](/home/worker/repo/app/page.tsx) so the home-page form generates the kit first and then routes to `/results/[kit_id]`, which is required before starting checkout.
- 2026-04-21: Added [`.env.example`](/home/worker/repo/.env.example) documenting the current required local env vars, including `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, and `RESEND_API_KEY`.
- 2026-04-21: Verified `npm run lint` and `npm run build` pass after the Stripe/refresh refactor.
- 2026-04-21: Ran a local runtime sanity check by seeding unpaid and paid `sponsorship_kits` rows, confirming `GET /api/kits/3` returns the free gated payload, confirming `GET /api/kits/4` returns the full paid payload with `refresh_eligible: true`, and confirming `POST /api/download-pack` for kit `4` returns `200 application/zip` containing exactly `media_kit.pdf`, `pitch_emails.txt`, and `brand_list.csv`.
- 2026-04-20: Built `/api/generate-kit` as the core AI sponsorship-kit pipeline with 90-day Neon caching, free/paid output gating, and a single GPT-4o structured JSON call.
- 2026-04-20: Added shared backend modules in [`lib/db.ts`](/home/worker/repo/lib/db.ts), [`lib/sponza/utils.ts`](/home/worker/repo/lib/sponza/utils.ts), [`lib/sponza/scrape.ts`](/home/worker/repo/lib/sponza/scrape.ts), [`lib/sponza/llm.ts`](/home/worker/repo/lib/sponza/llm.ts), and [`lib/sponza/service.ts`](/home/worker/repo/lib/sponza/service.ts).
- 2026-04-20: Extended [`app/api/webhooks/nanocorp/route.ts`](/home/worker/repo/app/api/webhooks/nanocorp/route.ts) to persist payment unlocks, and updated [`app/api/submit/route.ts`](/home/worker/repo/app/api/submit/route.ts) to use shared DB/platform helpers plus `niche_notes`.
- 2026-04-20: Verified `npm run lint` and `npm run build` both pass after the pipeline changes.
- 2026-04-20: Ran a local API sanity test by seeding a cached kit row, confirming `/api/generate-kit` returns the free gated payload before payment, then posting a mock NanoCorp webhook and confirming the same request returns the full paid payload from cache.
- 2026-04-20: Created Stripe products `GetSponza Sponsorship Kit` ($29 one-time) and `GetSponza Kit Refresh` ($19 one-time) with full descriptions via `nanocorp products create`.
- 2026-04-20: Deactivated legacy Stripe product `GetSponza Complete Sponsorship Kit` so the shared payment link only exposes the intended paid tiers.
- 2026-04-20: Confirmed the active NanoCorp catalog contains exactly the two requested products and that NanoCorp issued a new active payment link URL.
- 2026-04-20: Attempted browser-level verification of the Stripe checkout page with `agent-browser`, but the local environment does not have a Chrome binary installed, so checkout-page rendering was not visually verified.
- 2026-04-20: Audit for paid sponsorship-pack delivery found that the backend currently stops at JSON kit generation; there is no downloadable PDF/ZIP artifact route yet, the webhook only records unlocks, and [`app/results/page.tsx`](/home/worker/repo/app/results/page.tsx) is still hardcoded mock UI rather than rendering `/api/generate-kit` responses.
- 2026-04-20: Installed project dependencies plus `@react-pdf/renderer` and `jszip`, then read the local Next 16 App Router docs at [`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md) and [`node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md`](/home/worker/repo/node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md) before implementing new route handlers and client-side query-param usage.
- 2026-04-20: Built paid deliverable generation with [`lib/sponza/deliverables.tsx`](/home/worker/repo/lib/sponza/deliverables.tsx), including a professionally styled media-kit PDF and a ZIP bundle containing `media_kit.pdf`, `pitch_emails.txt`, and `brand_list.csv`.
- 2026-04-20: Added new Node runtime endpoints [`app/api/generate-pdf/route.ts`](/home/worker/repo/app/api/generate-pdf/route.ts) and [`app/api/download-pack/route.ts`](/home/worker/repo/app/api/download-pack/route.ts), plus pack-status tracking columns on `sponsorship_kits`.
- 2026-04-20: Extended [`lib/sponza/service.ts`](/home/worker/repo/lib/sponza/service.ts) to return `kit_id`, `download_ready`, and `pack_ready_at` in `/api/generate-kit` responses and to expose paid-kit lookup helpers for download routes and webhook processing.
- 2026-04-20: Replaced the mock results page with a live client flow in [`app/results/results-client.tsx`](/home/worker/repo/app/results/results-client.tsx) that fetches `/api/generate-kit`, polls for webhook-based unlocks, pre-fills Stripe checkout with the creator email, and shows a single `Download Your Sponsorship Pack` button for paid kits.
- 2026-04-20: Updated the landing-page hero form in [`app/page.tsx`](/home/worker/repo/app/page.tsx) to capture optional email and niche notes, and corrected the hardcoded NanoCorp payment link to the current active URL `https://buy.stripe.com/aFaeVe3He16v6iC2SneP03g`.
- 2026-04-20: Updated [`app/api/webhooks/nanocorp/route.ts`](/home/worker/repo/app/api/webhooks/nanocorp/route.ts) to prime the sponsorship pack after `checkout.session.completed`, marking `pack_ready_at` on success and recording failures on the kit row if generation fails.
- 2026-04-20: Verified `npm run lint` and `npm run build` pass after the sponsorship-pack changes.
- 2026-04-20: Ran a local end-to-end sanity test by seeding a paid `sponsorship_kits` row, starting the built app locally, confirming `POST /api/generate-pdf` returns `200` with `Content-Type: application/pdf`, confirming `POST /api/download-pack` returns `200` with `Content-Type: application/zip`, and verifying the ZIP contains exactly `media_kit.pdf`, `pitch_emails.txt`, and `brand_list.csv`.
- 2026-04-20: Installed Chrome via `agent-browser install` and used `agent-browser` to open the local `/results` page for a seeded paid kit, confirming the live UI renders the sponsorship report plus the `Download Your Sponsorship Pack` buttons.

## Sponsorship Kit Pipeline
- Input: `{ url, email, niche_notes? }`
- Supported platforms: YouTube, Instagram, TikTok
- YouTube path: resolves a channel from channel/video/handle URLs, fetches channel stats via YouTube Data API v3, loads recent uploads, estimates avg views and engagement, and passes recent title themes into the prompt.
- Instagram/TikTok path: tries public oEmbed first, then falls back to public page metadata scraping; TikTok also tries `yt-dlp` if available at runtime.
- Failure behavior: scraping degrades to URL + niche notes only; LLM errors return `We couldn't analyze this URL — try again or paste a different URL`; all failures are logged to `kit_failures`.
- Free response: readiness score/label/insights, full creator profile, rate-card range only, first 3 brand matches plus remaining count, first pitch email truncated to 100 chars, media-kit summary, and `last_analyzed`.
- Paid response: full stored kit JSON plus `last_analyzed`.

## Environment Notes
- Local shell currently has `DATABASE_URL` set.
- Local shell currently does **not** have `OPENAI_API_KEY`, `YOUTUBE_API_KEY`, or `RESEND_API_KEY` set.
- Required env vars after the payment-link refactor are now exactly: `OPENAI_API_KEY`, `YOUTUBE_API_KEY`, `DATABASE_URL`, and `RESEND_API_KEY`.
- Because only `DATABASE_URL` is present locally, the manual unlock route, generation routes, and build were verified structurally, but live AI generation and real outbound confirmation emails were not exercised end-to-end in this session.

## Results Page
- Canonical route is now `/results/[kit_id]`
- The page fetches `/api/kits/[kit_id]` so it always reflects the DB’s paid/free state for that exact kit row
- Free kits show the Readiness Score plus blurred paid sections and a `Get Your Full Kit — $29` CTA
- Clicking the payment CTA opens the NanoCorp payment link in a new tab and reveals the secondary “Already paid?” email unlock form on the same page
- Paid kits show the full content plus `Download Your Sponsorship Pack`
- Paid kits also show the 90-day refresh card; once eligible, it creates a brand-new refresh kit, routes to `/results/[new_kit_id]?unlock=1`, and opens the same payment link in a new tab

## Next Steps (Follow-up Tasks)
1. **Real payment QA** — run one real purchase through the shared payment link from `/results/[kit_id]`, then verify `/api/unlock-kit` unlocks the correct kit only when the stored email matches.
2. **Refresh QA with live generation** — after `OPENAI_API_KEY` and `YOUTUBE_API_KEY` are set, validate that `/api/refresh-kit` creates a brand-new kit row and that the `$19` refresh path lands on the new kit page with the unlock prompt visible.
3. **Fraud hardening** — replace the MVP email-match unlock with a stronger payment reconciliation flow once revenue justifies a dedicated checkout/webhook setup or a signed payment reference.
4. **Artifact persistence** — decide whether paid packs should remain on-demand only or be stored in Blob storage with signed URLs for faster repeat downloads and support workflows.
5. **Legacy cleanup** — once production traffic no longer needs the compatibility paths, remove [`app/api/webhooks/nanocorp/route.ts`](/home/worker/repo/app/api/webhooks/nanocorp/route.ts), [`app/api/submit/route.ts`](/home/worker/repo/app/api/submit/route.ts), and any remaining compatibility logic around `/results`.
